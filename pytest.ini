[pytest]
testpaths = tests

# strict mode + --strict-config means a missing pytest-asyncio makes the run
# fail on an unknown "asyncio_mode" option instead of silently skipping the
# async tests as pytest would otherwise do.
addopts = --strict-config --strict-markers
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
